//! Streaming fragment helpers. Currently empty — the append logic lives in
//! `session::Session::append_svg_fragment`. Reserved for future helpers such as
//! batched fragment validation and JSON envelope decoding (SPEC §8.1) once
//! the wire-protocol convenience layer lands.
